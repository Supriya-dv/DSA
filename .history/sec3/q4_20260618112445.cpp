// Input age and check if age is between 18 and 60 using &&.

#include<iostream>
using namespace std;
int main(){
    int age;
    cin >> age;
    if(age  > 18 && age < 60)
    cout << "Age is Valid" << age <<endl;
    else
    cout << "N"
    return 0;

}
